$IF DEFINED(MDSGOLD_MAIN)
      INTEGER*2 LAST_VARIABLE
$ELSE
      INTEGER*2 LAST_VARIABLE
      REAL VARIABLE_VALUE(0:500),OUTPUT_VARIABLE(500)
$ENDIF
      LOGICAL*1 SHORT_FORM,VARIABLE_ACTIVE(0:500)
