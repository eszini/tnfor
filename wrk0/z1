/*
 *
 *	prue3
 *
 *	Descripcion:
 *
 */


/* bloque */
#if 0


int	pro_prue3()
{
	int	i,j,k;
	int	f1,f2,f3;
	char	b1[MAXB];
	char	b2[MAXB];
	FILE	*hwi;



	char	z[MAXV];
	sprintf (z,"prue3");

	/* proceso */
	if (gp_fverbose("d1"))
	{	printf ("%s%s%s\n\n",gp_tm(),gp_m[0],z);
	}

	if (!1 || !2 )
		gp_uso(11);

	/* bloque */
		
	/* proceso */
	if (gp_fverbose("d1"))
	{	printf ("%s%s%s\n\n",gp_tm(),gp_m[1],z);
	}
}


#endif
/* bloque */

